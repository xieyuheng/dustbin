;;; Input

(claim main (-> void-t))

(define (main)
  (= y 6)
  (begin
    (= x (begin (= y (ineg 42)) y))
    (println (iadd x y))))

;;; Loaded

(claim main (-> void-t))

(define (main)
  (= y 6)
  (begin
    (= x (begin (= y (ineg 42)) y))
    (println (iadd x y))))

;;; ShrinkPass

(claim main (-> void-t))

(define (main)
  (= y 6)
  (= x (begin (= y (ineg 42)) y))
  (println (iadd x y)))

;;; UniquifyPass

(claim main (-> void-t))

(define (main)
  (= y₁ 6)
  (= x₁ (begin (= y₂ (ineg 42)) y₂))
  (println (iadd x₁ y₁)))

;;; LiftLambdaPass

(claim main (-> void-t))

(define (main)
  (= y₁ 6)
  (= x₁ (begin (= y₂ (ineg 42)) y₂))
  (println (iadd x₁ y₁)))

;;; UnnestOperandPass

(claim main (-> void-t))

(define (main)
  (= y₁ 6)
  (= x₁ (begin (= y₂ (ineg 42)) y₂))
  (= _₁ (iadd x₁ y₁))
  (println _₁))

