;;; Input

(claim main (-> void-t))

(define (main)
  (= x (begin (= x 4) (iadd x 1)))
  (println (iadd x 2)))

;;; Loaded

(claim main (-> void-t))

(define (main)
  (= x (begin (= x 4) (iadd x 1)))
  (println (iadd x 2)))

;;; ShrinkPass

(claim main (-> void-t))

(define (main)
  (= x (begin (= x 4) (iadd x 1)))
  (println (iadd x 2)))

;;; UniquifyPass

(claim main (-> void-t))

(define (main)
  (= x₁ (begin (= x₂ 4) (iadd x₂ 1)))
  (println (iadd x₁ 2)))

;;; LiftLambdaPass

(claim main (-> void-t))

(define (main)
  (= x₁ (begin (= x₂ 4) (iadd x₂ 1)))
  (println (iadd x₁ 2)))

;;; UnnestOperandPass

(claim main (-> void-t))

(define (main)
  (= x₁ (begin (= x₂ 4) (iadd x₂ 1)))
  (= _₁ (iadd x₁ 2))
  (println _₁))

