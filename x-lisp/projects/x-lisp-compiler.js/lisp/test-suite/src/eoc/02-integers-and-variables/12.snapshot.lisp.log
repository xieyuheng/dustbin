;;; Input

(claim main (-> void-t))

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; Loaded

(claim main (-> void-t))

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; ShrinkPass

(claim main (-> void-t))

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; UniquifyPass

(claim main (-> void-t))

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; LiftLambdaPass

(claim main (-> void-t))

(define (main)
  (println (iadd (iadd 1 2) (iadd 3 (iadd 4 5)))))

;;; UnnestOperandPass

(claim main (-> void-t))

(define (main)
  (= _₁ (iadd 1 2))
  (= _₂ (iadd 4 5))
  (= _₃ (iadd 3 _₂))
  (= _₄ (iadd _₁ _₃))
  (println _₄))

