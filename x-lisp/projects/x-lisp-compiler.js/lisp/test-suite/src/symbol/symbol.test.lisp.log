;;; Input

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; Loaded

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; ShrinkPass

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; UniquifyPass

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; LiftLambdaPass

(claim main (-> void-t))

(define (main)
  (assert-equal 'abcdef (symbol-append 'abc 'def)))

;;; UnnestOperandPass

(claim main (-> void-t))

(define (main)
  (= _₁ (symbol-append 'abc 'def))
  (assert-equal 'abcdef _₁))

