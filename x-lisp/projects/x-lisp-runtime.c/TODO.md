# later

[runtime] gc -- mark and sweep

[runtime] `make_tael`
[runtime] `make_curry`
[runtime] `make_string`

# later

[runtime] `x_write` -- need string

[runtime] `x_random_int`
[runtime] `x_random_float`
